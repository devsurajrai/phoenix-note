export const sortNotes=(notes,sortFilterState)=>{
    return notes
    // if(sortFilterState.sort==="LATEST_TO_OLDEST"){
    //     return notes.
    // }
    // else if(sortFilterState.sort==="OLDEST_TO_LATEST"){

    // }
    // else if(sortFilterState.sort===""){

    // }
    // else if(sortFilterState.sort==="OLDEST_TO_LATEST"){

    // }
    // else{

    // }
}